window.SIDEBAR_ITEMS = {"struct":[["AuthoredBlocks","For each session index, we keep a mapping of `ValidatorId<T>` to the number of blocks authored by the given authority."],["HeartbeatAfter","The block number after which it’s ok to send heartbeats in the current session."],["Keys","The current set of keys that may issue a heartbeat."],["ReceivedHeartbeats","For each session index, we keep a mapping of `SessionIndex` and `AuthIndex` to `WrapperOpaque<BoundedOpaqueNetworkState>`."]]};