window.SIDEBAR_ITEMS = {"struct":[["CandidateExists","A Map of the candidates. The information in this Map is redundant to the information in the `Pool`. But the Map enables us to easily check if a candidate is already in the pool, without having to iterate over the entire pool (the `Pool` is not sorted by `T::AccountId`, but by `T::Score` instead)."],["MemberCount","Size of the `Members` set."],["Members","The current membership, stored as an ordered Vec."],["Pool","The current pool of candidates, stored as an ordered Bounded Vec (ordered descending by score, `None` last, highest first)."]]};