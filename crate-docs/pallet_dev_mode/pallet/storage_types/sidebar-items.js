window.SIDEBAR_ITEMS = {"struct":[["Bar","The Hasher requirement is skipped by `dev_mode`. So, second parameter can be `_` and `Blake2_128Concat` is used as a default. When the dev_mode is removed, we would need to specify the hasher like so: `pub type Bar<T: Config> = StorageMap<_, Blake2_128Concat, T::AccountId, T::Balance>;`."],["Dummy","The MEL requirement for bounded pallets is skipped by `dev_mode`. This means that all storages are marked as unbounded. This is equivalent to specifying `#[pallet::unbounded]` on this type definitions. When the dev_mode is removed, we would need to implement implement `MaxEncodedLen`."]]};